namespace P2108Comparer
{
    enum PlotMode
    {
        Single,
        MultipleFrequencies,
        MultipleBaseStationHeights,
        MultipleElevationAngles,
        MultipleSheildingHeights
    }

    enum Scenerio : int
    {
        LowRise = 10,
        MidRise = 20,
        HighRise = 30
    }

    enum Antenna
    {
        Isotropic,
        Directional
    }
}
